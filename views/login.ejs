<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css" rel="stylesheet">
</head>
<body class="h-screen bg-gray-100 flex items-center justify-center">
  <div class=" bg-white rounded-lg shadow-xl overflow-hidden">
    <div class="flex h-full">
      <!-- Login Form Section -->
      <div class="w-1/2 bg-white shadow-lg p-4 overflow-y-auto">
        <h2 class="text-lg font-bold mb-4">Login</h2>
        <form id="login-form" class="space-y-4">
          <div class="flex items-center space-x-2">
            <i class="ri-mail-line text-lg text-gray-500"></i>
            <input 
              type="email" 
              id="email-input" 
              class="flex-1 p-2 border rounded-lg focus:outline-none focus:ring focus:ring-blue-300" 
              placeholder="Email">
          </div>
          <div class="flex items-center space-x-2">
            <i class="ri-lock-line text-lg text-gray-500"></i>
            <input 
              type="password" 
              id="password-input" 
              class="flex-1 p-2 border rounded-lg focus:outline-none focus:ring focus:ring-blue-300" 
              placeholder="Password">
          </div>
          <button 
            type="submit" 
            class="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 focus:outline-none">
            Login
          </button>
        </form>
        <div class="flex items-center space-x-2 mt-4">
          <p class="text-sm text-gray-500">Don't have an account?</p>
          <a href="#" class="text-sm text-blue-500 hover:text-blue-600">Sign up</a>
        </div>
      </div>
      <!-- Google Sign In Section -->
      <div class="w-1/2 bg-white shadow-lg p-4 overflow-y-auto">
        <h2 class="text-lg font-bold mb-4">Sign in with Google</h2>
       <a href="/auth/google"> <button 
          type="button" 
          class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 focus:outline-none">
          <i class="ri-google-fill text-lg text-white"></i>
          Sign in with Google
        </button></a>
      </div>
    </div>
  </div>
</body>
</html>

